// Package help prints what the binary can be asked to do.
//
// It is a command like any other — the binary tells the caller to reach for it
// by name, so something has to own that name — and it is the one command that
// needs nothing but a writer to run.
package help
